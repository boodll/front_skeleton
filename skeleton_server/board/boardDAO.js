const getPool = require('../common/pool')

//이곳에 필요한 sql등록
const sql ={
  boardList: '',
}

const boardDAO = {
  //게시물 조회 요청 왔을때 router 에 의해 실행 dbms
  boardList: async (callback) =>{

  }
}
module.exports = boardDAO